# Core dependencies for October 2025 deployment
# Pinned versions for stability# Core Dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.4.2
python-multipart==0.0.6

# Security Dependencies
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==42.0.2
bcrypt==4.1.2
redis==4.6.0  # For rate limiting (compatible with celery if needed)
email-validator==2.1.1  # For Pydantic email field validation
# ML and NLP
numpy==1.26.4  # Compatible with Python 3.10 and 3.11
sentence-transformers==2.2.2
torch==2.5.1  # Pinned to avoid weights_only issue with YOLOv8
transformers==4.35.0
nltk==3.8.1
# Vector search for pricing integration and spec embeddings
faiss-cpu==1.8.0
# PDF processing - enhanced with PDFPlumber
pypdf==3.17.0
pdfplumber==0.10.3  # Better table and formatting extraction
pytesseract==0.3.10
Pillow==10.1.0
# NLP for chunking
nltk==3.8.1
# Data processing for pricing integration and mega bundle analysis
pandas==2.2.0  # Compatible with numpy 1.26.4
# Geographic clustering for job scheduling
scikit-learn==1.3.2
geopy==2.4.0
# LoRA for efficient fine-tuning
peft==0.11.1
# Evaluation metrics for NER
evaluate==0.4.0
seqeval==1.2.2
# Excel report generation
openpyxl==3.1.2
# PDF report generation  
reportlab==4.0.9
# Visualization for dashboard
plotly==5.18.0
streamlit==1.29.0
# Database (only if using PostgreSQL)
psycopg2-binary==2.9.9
# Celery with Redis support (includes compatible redis version)
celery[redis]==5.3.4
# Note: redis package is included with celery[redis] at version ~4.5.x
# PDF manipulation for as-built processing
reportlab==4.0.9
PyPDF2==3.0.1
# Computer vision for pole detection
ultralytics==8.0.200
opencv-python-headless==4.8.0.74  # Required by roboflow, headless for server
roboflow==1.1.9  # Roboflow SDK for pre-trained models
sqlalchemy==2.0.23
boto3==1.28.62
psutil==5.9.5  # For memory monitoring
# Already included with sentence-transformers but explicitly listing:
# numpy (for pole classification calculations)
# torch (CPU version already included above)

# Fine-tuning dependencies for poles/underground NER
# peft==0.11.1 already included above on line 28
datasets>=3.0.0  # Hugging Face datasets for training (compatible with Python 3.13)
accelerate==0.24.0  # Training acceleration
# scikit-learn==1.3.2 already included above on line 25
pyarrow>=17.0.0  # Python 3.13 compatible version for datasets
